# Python Dependencies for Railway Deployment
# Core dependencies
python-dotenv==1.0.0
urllib3==2.0.7

# Optional dependencies for enhanced features
psutil==5.9.6  # For system metrics (optional)

# Development dependencies (not needed in production)
# pytest==7.4.3
# pytest-asyncio==0.21.1
